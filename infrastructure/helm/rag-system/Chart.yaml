# =============================================================================
# Helm Chart for RAG+LLM System
# =============================================================================
# AI Learning Note:
# Helm is the package manager for Kubernetes. Charts are reusable templates
# that can be customized per environment (dev, staging, prod).
# =============================================================================
apiVersion: v2
name: rag-system
description: Production RAG+LLM System with Azure OpenAI and AI Search
type: application
version: 1.0.0
appVersion: "1.0.0"

keywords:
  - rag
  - llm
  - azure-openai
  - fastapi
  - kubernetes

maintainers:
  - name: Siva
    email: siva@example.com

dependencies: []
